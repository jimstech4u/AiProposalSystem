So now let apply the fix.
1. use NGN (₦)
2. Generate the latest Screenshots (Figures 24–30) from the latest today website preview through playwright into /thesis/corrected_images.
4. For all the UML diagrams, have a UML_prompt.md that has the prompt to correctly create each and also try to Generate a high quality UML correct disgram fow the wrong one into /thesis/corrected_images.
5. Also let have a new corrected_info.md that correctly has the Data Dictionary (Tables 16–21) uses MySQL types and wrong fields — the real DB is PostgreSQL, System Architecture prose, "Technologies and Tools Used" table, it is actually an LLM API integration, "Sales" is missing everywhere, Software Testing — references, Hardware & Software Requirements, Sequence diagram descriptions 